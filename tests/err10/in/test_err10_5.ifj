const ifj = @import("ifj24.zig");

pub fn test(x: i32) i32 {
    if (x > 0) {
        return 42;
    } else {
        return "error";  // vrací string místo i32
    }
}

pub fn main() void {
    _ = test(5);
}