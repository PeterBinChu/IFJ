const ifj = @import("ifj24.zig");

pub fn main() void {
    var x: i32 = 5;
    var y: i32 = 10;
    var sum = x + y;
    var sub = y - x;
    var mul = x * y;
    var div = y / x;
    ifj.write(sum);
}