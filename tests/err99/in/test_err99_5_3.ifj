const ifj = @import("ifj24.zig");
pub fn main() void {
 for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) { for (var x: i32 = 0; x < 1; x++) {    }
}
